<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Community</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>Explore Our Community</h1>

    <p>Connect with others and explore their pantries!</p>

    <h2>Community Members:</h2>

    <% if (users.length === 0) { %>
        <p>No users found.</p>
    <% } else { %>
        <ul>
            <% users.forEach(viewedUser => { %>
                <% if (viewedUser.username) { %>
                <li>
                    <a href="/users/<%= viewedUser._id %>"><%= viewedUser.username %></a>
                </li>
                <% } %>
            <% }) %>
        </ul>
    <% } %>    
</body>
</html>